// This form now lives in features/records since it maps directly to the
// real health-records API — re-exported here since the onboarding sign-up
// flow's "upload a record" step still points at this path.
export { default } from "../../records/components/UploadRecordForm";
